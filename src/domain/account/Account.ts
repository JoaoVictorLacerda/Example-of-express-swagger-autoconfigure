export default abstract class Account {
    _id: string;
    name: string;
    birthDate: string;
    email: string;
    password: string;
    rules: string[];
    type: string;
    cep: string;
    city: string;
    state: string;
}

